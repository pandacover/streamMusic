type PropsType = {
	isPlaying: boolean;
	isAutoLoop: boolean;
	volumeIndex: number;
	progressIndex: number;
};

export const Controls = ({
	isPlaying,
	isAutoLoop,
	volumeIndex,
	progressIndex,
}: PropsType) => {
	return <div></div>;
};
